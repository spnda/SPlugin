plugins {
    id 'application'
}

dependencies {
    // Spigot is the easiest way to build against the Bukkit API and for it to still work with Paper and other Spigot forks
    compileOnly 'org.spigotmc:spigot-api:1.19-R0.1-SNAPSHOT' // Spigot
}

compileKotlin.kotlinOptions.jvmTarget = "1.8"
